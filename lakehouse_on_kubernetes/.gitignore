# Synced-in artifacts from databricks_lakehouse (regenerated by sync-lakehouse.sh).
# Kept out of git so the source project stays the single source of truth.
lakehouse/src/
lakehouse/config/table_registry.yaml
lakehouse/generate_source_data.py
lakehouse/init_ods.sql

# ConfigMap manifests rendered from the synced files (see scripts/sync-lakehouse.sh).
# The hand-authored minio-creds.yaml is committed; these two are generated.
k8s/config/lakehouse-config.generated.yaml
k8s/config/pg-init.generated.yaml

# Python
__pycache__/
*.pyc
.venv/

# Local env
.env
